#!/bin/bash

cd <%= node['sandy']['home'] %>/current

exec 2>&1
exec chpst -u processing -e /etc/sv/sidekiq/env \
  chruby-exec <%= node['sandy']['ruby']['version'] %> -- bundle exec sidekiq -t 30 -e production